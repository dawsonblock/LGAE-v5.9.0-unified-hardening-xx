# v3.1 merge notes

Canonical base: the former full geometry-governor archive.

Transplanted from the smaller geometric-controller archive:

- sparse edge-list Γ/radius/local-variance kernels;
- compile-friendly task/diffusion/fiber training core;
- fixed padded edge-buffer training helper;
- explicit compiled-arithmetic/eager-evolution separation;
- training-step integration pattern.

The smaller archive's curvature stubs and buffer-only gate implementation were not retained.

The v3.1 hardening pass additionally corrected the WAF3-pressure contract, Bakry–Émery nullspace handling, entropic failure semantics, mutation invariants, component protection, shadow rollout, governed fiber transitions, persistent-homology wiring, state/version identity, stale quarantine handling, checkpoint persistence, role plumbing, and explicit weighted/unweighted curvature boundaries.
